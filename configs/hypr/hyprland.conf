# ================================
# Hyprland Base Config (Validated)
# ================================

# -------- Environment Variables (Wayland / Toolkits / Cursor) --------
env = XDG_SESSION_TYPE,wayland
env = XDG_CURRENT_DESKTOP,Hyprland

env = MOZ_ENABLE_WAYLAND,1
env = QT_QPA_PLATFORM,wayland
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1

# Cursor theme + size (Wayland + XWayland)
env = HYPRCURSOR_THEME,Bibata-Modern-Ice
env = HYPRCURSOR_SIZE,24
env = XCURSOR_THEME,Bibata-Modern-Ice
env = XCURSOR_SIZE,24

# -------- Config Imports (modular layout) --------
source = ~/.config/hypr/keybinds.conf
source = ~/.config/hypr/monitors.conf
source = ~/.config/hypr/autostart.conf

# -------- General Desktop Behaviour --------
general {
    gaps_in = 6
    gaps_out = 12
    border_size = 2
    layout = dwindle
    no_border_on_floating = true
    allow_tearing = false
}

# -------- Decoration (rounding only) --------
decoration {
    rounding = 8
}

# -------- Shadow (must be separate block; 0 = disabled) --------
shadow {
    strength = 0.0
    range = 0
    color = #00000000
}

# -------- Animations (minimal but smooth) --------
animations {
    enabled = yes

    # animation = type, duration(ms), easing, style
    animation = windows, 120, 6, default
    animation = border, 120, 5, default
    animation = fade, 150, 6, default
}

# -------- Input (keyboard / mouse) --------
input {
    kb_layout = us
    kb_options =
    follow_mouse = 1
    sensitivity = 0

    repeat_delay = 300
    repeat_rate = 40

    mouse_refocus = false
}

# -------- Cursor Block (runtime control flags only) --------
cursor {
    no_hardware_cursors = false
}

# -------- Window Rules (examples) --------
windowrulev2 = float, class:^(pavucontrol)$
