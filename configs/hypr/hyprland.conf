# Hyprland base config (minimal ml4w-style)

# Imports
source = ~/.config/hypr/keybinds.conf
source = ~/.config/hypr/monitors.conf
source = ~/.config/hypr/autostart.conf

# Basic settings
general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
    layout = dwindle
}
